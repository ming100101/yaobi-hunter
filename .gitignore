# dependencies
node_modules/

# build output
dist/
scripts/.build/

# packaged executable (90 MB — the embedded Node runtime; rebuild with scripts/make-exe.mjs)
sea/

# backtest market-data cache (refetchable via npm run backtest -- --refresh)
backtest-data/

# logs / os cruft
*.log
.DS_Store
Thumbs.db

# local runtime data and secrets
.env
.env.*
!.env.example
kv.json
recordings/
entry-watch.json
deep-reclaim.json
market-1h.json
strategy-shadow.json
strategy-lab.json
KILL

# generated public release archives
release/.stage/
release/.stage-build/
release/*.zip
release/SHA256SUMS.txt
